import { Text, View } from 'react-native';
import { Screen } from '@/components/Screen'; import { palette, radius } from '@/constants/Colors'; import { useColorScheme } from '@/components/useColorScheme';
export default function CreateTransaction(){const c=palette[useColorScheme()??'light'];return <Screen><Text style={{fontFamily:'Inter_800ExtraBold',fontSize:28,color:c.text}}>Novo lançamento</Text><View style={{backgroundColor:c.surface,borderColor:c.border,borderWidth:1,borderRadius:radius.card,padding:20,gap:12}}><Text style={{fontFamily:'Inter_700Bold',color:c.text}}>O registro de receitas e despesas será conectado ao BFF na próxima etapa.</Text><Text style={{fontFamily:'Inter_400Regular',color:c.textMuted}}>Esta tela já reserva o fluxo nativo em formato de sheet.</Text></View></Screen>}
